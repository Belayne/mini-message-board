{
  "name": "express-message-board",
  "version": "1.0.0",
  "description": "A mini message board made with express and ejs",
  "main": "app.js",
  "type": "module",
  "engines": {
    "node": ">=20.11.x"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "start-dev": "node --watch --env-file=.env app.js & npx @tailwindcss/cli -i ./input.css -o ./public/styles.css --watch",
    "populatedb": "node ./db/populatedb.js"
  },
  "author": "Naol Derara",
  "license": "ISC",
  "dependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "pg": "^8.13.1",
    "tailwindcss": "^4.0.0"
  }
}
